/** Shared between TaskItem and its drag preview, so a lifted row keeps its priority colour. */
export const priorityClasses: Record<number, string> = {
  1: 'text-accent',
  2: 'text-priority-2',
  3: 'text-priority-3',
  4: 'text-ink-light',
};
